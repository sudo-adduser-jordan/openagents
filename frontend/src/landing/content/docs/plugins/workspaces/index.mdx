---
title: Workspaces
description: Git worktrees for repositories and Open Agents-managed directories for Scratch projects.
---

Workspace behavior follows the registered project kind:

| Project kind | Session workspace |
| --- | --- |
| Git repository | A fresh [Git worktree](/docs/plugins/workspaces/worktree) and branch. |
| Multi-repository workspace | A root worktree with registered child repositories materialized for the session. |
| Scratch | An Open Agents-managed directory with no branch or PR operations. |

Open Agents does not currently offer a full-clone workspace selector. Register a project with `open-agents project add`, then let the daemon route workspace operations from its project record.

Workspace cleanup is conservative: killing a session does not force-delete a dirty registered worktree. Use `open-agents session cleanup --dry-run` to inspect eligible terminated sessions.
