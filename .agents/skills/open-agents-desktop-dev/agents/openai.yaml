interface:
  display_name: "Open Agents Desktop Dev"
  short_description: "Run the real Open Agents Electron app from a checkout"
  default_prompt: "Use $open-agents-desktop-dev to launch this checkout’s Electron app against real local Open Agents data and verify the requested UI."
